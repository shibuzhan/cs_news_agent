# 进度记录

## 2026-09-11｜Phase 53 开始

- 用户确认实施：异步化人工触发自动审核、明确图片大小限制、GitHub 文案取消正文原文标题尾注；并询问审核与投递是否适合作为子 Agent 能力。
- 安全边界：不读取或修改私有 `.env`，不调用真实模型、图片、搜索或微信公众号接口；仅修改本地应用、测试与容器构建产物。
- Python 语法编译与前端 TypeScript 静态检查已通过。项目 `.venv` 的 pytest 启动再次被 uv trampoline 的 `os error 5` 阻止；不重复该路径，改用现有应用容器做隔离替身验证。

## 2026-09-11｜Phase 53 完成

- 手动审核接口现在创建 `queued` 审核运行并返回；ARQ 的 `process_auto_review_job` 在后台完成审核、最多一次改稿及草稿箱投递。重复点击会返回明确冲突，不会并发创建两个审核流程。
- 同步 OpenAI 审核/改稿调用在 Worker 线程池中执行，审核所需草稿字段在事件循环线程制作只读快照，避免线程使用 SQLAlchemy Session。
- 正文插图超过 1 MB 时会显示文件名、实际体积以及“本地预检、尚未请求公众号接口”；封面永久素材上传无同一脚本限制，并记录大小但不记录图片内容。
- GitHub 正文不再附加“原文标题”尾注；生成、确定性生成、改稿、重生成和审核规则均按来源区分，项目名继续保留在文章主标题。
- 验证：Python 语法编译、前端 TypeScript、前端生产构建通过；app/worker/frontend 均运行，Worker 启动日志确认注册 6 个函数（含 `process_auto_review_job`），容器内格式与任务注册断言、`/docs` 和前端 HTTP 均返回 200。未调用真实模型、微信、图片或搜索服务。
- 测试限制：宿主 `.venv` pytest 被 uv trampoline `os error 5` 阻止；应用镜像未安装 pytest（`No module named pytest`），因此新增专项 pytest 未在当前环境执行，未安装依赖或修改环境。

## 2026-09-10｜Phase 49 原记录重试超时隔离

- 已开始：为复用生成记录建立独立的本次尝试开始时间，避免旧创建时间触发错误的遗留任务收束。
- 已完成：新增 `0022_run_attempt_started` 迁移，将既有记录的本次尝试时间初始化为首次创建时间；原记录重开时刷新该字段，遗留收束仅按该字段判断。app 与 worker 已重建，迁移和回归检查通过。

## 2026-09-10｜Phase 50 自然正文生成

- 已完成：初稿生成和自动改稿统一使用自然正文 `body`，以 4–8 个自然段作为文章形态要求，不再要求模型返回四项逻辑部分数组。旧草稿的逻辑部分审计保留用于历史查看，但不再作为自动审核失败门槛。

## 2026-09-11｜Phase 51 开始

- 用户要求：修复发布页审核意见对象渲染；将四类来源文案生成拆为独立 Skill；GitHub 标题包含项目名；优化三阶段视觉；去掉过时附件提示；正文最低 1200 字并允许受控联网补充。
- 已完成只读定位：发布页和生成记录都直接渲染 `model_report.issues`，历史对象项会触发 React 错误；增强生成已有受控 Exa 搜索但当前仅用于术语解释。用户随后要求继续任务，按项目确认约定进入实施。

## 2026-09-11｜Phase 51 完成

- 发布页和生成记录的审核意见均使用可显示文本：后端会规范历史 `{type, description}`，前端也会在接收异常历史数据时防御性转换，避免 React 将对象作为子节点渲染。
- 已新增四类来源写作 Skill，并由生成器按来源读取；GitHub 标题服务端确保包含 `owner/repo`，正文最低有效字符数提升为 1200，受控 Exa 查询仍限制为最多两次且不允许任意 URL。
- 三阶段改为带图标、状态色和进行中动画的阶段卡片；已移除界面中的旧附件提示，未改变附件处理授权。
- 验证：前端 TypeScript 检查通过；纯函数测试 5 项通过；容器内断言确认 API 审核响应可渲染、Skill 被包含、GitHub 标题和 1200 字下限生效；本机前端与 API 均返回 HTTP 200。应用、Worker、前端容器运行正常。容器镜像未包含 pytest，本机 Anaconda 又缺少 `langsmith`，故增强生成整套 pytest 未在该环境运行；这属于测试依赖缺口。

## 2026-09-11｜Phase 52 开始

- 用户确认：将四类来源写作 Skill 整理为有按需 `references/` 的完整规则包；同时询问是否应把联网搜索作为子 Agent Tool。只实施 Skill 资料化，子 Agent 搜索仅做架构评估，等待单独确认。

## 2026-09-11｜Phase 52 完成

- 每个来源写作 Skill 新增 `references/evidence-and-search.md`，入口 `SKILL.md` 仅保留路由和核心约束；生成器会同时读取入口与明确链接的 reference，避免 reference 只存在于目录却不进入实际生成上下文。
- 未添加未接入运行链的 `scripts/` 占位目录。正文验证仍由服务端唯一实现，避免 Skill 脚本与运行时规则发生漂移。
- 验证：四个 Skill 在 UTF-8 模式下通过 Skill Creator 校验；`generator.py` 语法检查通过；重建 app/worker 后，容器内已确认 GitHub 以及四类来源的入口与 reference 均可读取。没有调用真实模型、Exa、图片或公众号服务。
- 架构评估：普通文案生成不宜默认改为搜索子 Agent。当前“结构化检索规划 + 受控 Exa Tool”权限更小、时延更低、审计更直接；只有将来明确需要多轮研究、来源交叉核验时，才考虑受限研究子 Agent。

## 2026-09-06｜公众号 MCP 部署

- 已开始 Phase 14：新增第三方 MCP 的内网容器定义、最小 SSE 适配器、公众号发布任务审计和发布情况页面。
- `docker compose config --quiet` 已通过；服务仅声明在内部 Docker 网络，未映射公众号 MCP 的主机端口。
- 首次在受限执行环境调用 `uv run` 无法查询 Python 解释器（访问被拒绝）；将使用项目 uv 环境的授权执行路径重试，不改动依赖。

## 2026-09-05

- 用户确认第一版范围及 PostgreSQL/pgvector 容器方案。
- 开始阶段 1：项目配置、领域模型和迁移。
- 完成阶段 1：依赖配置、领域模型、PostgreSQL 表、仓储和首个 Alembic 迁移。
- 开始阶段 2：实现四类采集器和统一数据格式。
- 完成阶段 2：arXiv Atom、GitHub daily/weekly Trending、HN 官方 API 和可配置 RSS 采集器。
- 开始阶段 3：实现内容处理服务、LLM 降级和 LangGraph。
- 完成阶段 3：清洗、精确去重、规则分类、热度计算、可选 OpenAI 兼容生成器和 LangGraph 处理管道。
- 开始阶段 4：实现 API、审核状态和容器运行方式。
- 完成阶段 4：FastAPI 采集/查询/编辑/审核接口、请求日志、幂等审核及 Docker Compose。
- 开始阶段 5：补充固定样例、离线测试、容器验证和 README。
- Python 静态编译通过；Docker 引擎可用，Compose 配置校验通过。
- 发现现有 `.venv` 无 pip，安装方式调整为 uv sync。
- `uv sync --extra dev` 完成，离线测试 8 项全部通过。
- 首次容器构建在下载基础镜像时被执行窗口中断，拆分构建与启动继续验证。
- 收尾测试 9 项全部通过。
- 构建并启动 FastAPI 与 PostgreSQL/pgvector 容器；迁移版本为 `0001_initial (head)`。
- `/api/health` 返回数据库已连接；固定样例完成生成、审核和重复审核幂等验证。
- 第一版全部阶段完成，容器保持运行。

## 2026-09-05｜真实采集可靠性阶段

- 用户确认实现采集运行记录、GitHub Trending 快照、来源健康接口和真实来源验收。
- 开始 Phase 6。
- 完成采集运行记录、逐来源失败隔离、来源健康接口和 GitHub daily/weekly 独立指标快照。
- 新增失败隔离测试，离线测试由 9 项增至 10 项且全部通过。
- 开始 Phase 7：容器迁移与真实来源验收。
- 迁移升级到 `0002_collection_reliability (head)`，容器健康检查通过。
- 首次真实采集发现现有 `.env` 启用了外部模型，立即重启应用中断请求；增加默认关闭的 `LLM_ENABLED` 安全开关。
- 安全开关测试通过，离线测试共 11 项全部通过；容器确认 `LLM_ENABLED=False`。
- 限量真实采集成功处理 arXiv、Hacker News 和 GitHub Changelog RSS，各生成 2 条待审核草稿；GitHub 失败被独立记录且未阻断其他来源。
- 确认 GitHub 失败原因为当前主机和容器把 `github.com` 解析到 `127.0.0.1`；未修改系统网络配置。
- 在 PostgreSQL 回滚事务中验证 daily/weekly 两条趋势快照的排名和增星分别正确，未保留验证数据。
- Phase 7 完成。
- 用户同意进入主 Agent/Tool 设计阶段；正在给出准确文件范围、状态流转、Tool 契约和验收边界，尚未修改业务代码。
- 用户已确认实施 Phase 8，开始创建受控主 Agent、四类内部采集 Tool、父级审计记录和离线测试。
- 已完成内部 Tool 与主 Agent 单元测试，离线测试共 16 项通过；README 已补充主 Agent 指令、审计层级和非 MCP 边界。
- 已构建并启动容器，迁移升级到 `0003_main_agent (head)`；`LLM_ENABLED=False` 已确认。
- `/api/agent/collect` 真实调用 arXiv 1 条成功，父级 Agent Run 与子级 Collection Run 关联一致；重复资讯按既有规则计为 duplicate。
- 原 `/api/collections/arxiv` 兼容入口真实调用成功。
- Phase 8 完成。
- 用户明确：文本文件默认只作为对话附件保存，只有明确指令才允许 LLM 调用 Tool 提取内容并生成待审核草稿；正在设计 Phase 9，尚未实现前端或对象存储。
- 用户已确认实施 Phase 9，开始构建私有 MinIO、聊天附件、明确指令门禁和 React 审核运营台。
- 新增 React + Vite + TypeScript + Tailwind CSS 运营台，完成对话附件保存/选择和草稿审核编辑页面；前端生产构建通过。
- 新增私有 MinIO 服务、聊天/附件/处理审计表、`0004_chat_attachments` 迁移、短时下载链接与文本提取 Tool。
- 明确附件提取门禁已实现：普通上传和消息不读取附件；明确指令仍必须满足 `LLM_ENABLED=true`，关闭时附件保持 `uploaded`。
- 新增附件与门禁单元测试；离线测试共 21 项通过。
- 容器迁移升级到 `0004_chat_attachments (head)`，前端、API、PostgreSQL 和 MinIO 均运行；固定 Markdown 完成上传、普通对话、关闭模型门禁和签名下载闭环。
- 已优化应用镜像依赖缓存层并重新部署；运行容器确认已加载审核时重签附件来源链接的修正，健康检查成功。
- Phase 9 完成。
- 修复前端 5173 端口跨域预检：保留用户既有 CORS 来源并追加本机 5173 来源；app 容器重建后，浏览器等价 OPTIONS 请求返回 200。

## 2026-09-05｜通用对话与计划 Tool 阶段

- 用户确认实现通用对话、意图识别、可展开执行过程摘要，以及定时/发布计划草稿。
- 开始 Phase 10：定时/发布操作将建模为受控 Tool；规则、确认条件和禁止项保留为可复用 Skill，不接入真实调度器或发布平台。
- 新增 ConversationAgent 单元测试首次收集因测试替身类型标注括号拼写错误失败；已记录并定点修正，未发生真实模型或外部调用。
- 已完成 ConversationAgent、四种受控意图、运行/事件/计划审计表及 `0005_conversation_agent` 迁移。
- 新增定时计划和发布计划的创建/确认 Tool 与 API；确认只写入审计状态，不会注册任务、登录账号或向平台发布。
- 对话页新增可折叠“执行过程摘要”；摘要展示的是意图、Tool 与确认状态，不包含模型原始思维链。计划确认后会同步回会话审计，刷新页面不会再次显示确认按钮。
- `uv run pytest` 24 项通过，Python 静态编译和前端生产构建通过；app、frontend、MinIO、PostgreSQL 容器运行，迁移为 `0005_conversation_agent (head)`，健康接口返回数据库已连接。
- Phase 10 完成。

## 2026-09-05｜聚合任务、会话历史与受控记忆阶段

- 用户确认修复 GitHub 草稿结构问题，改为并行处理前 5 项并生成一条总文案；确认增加 Redis 后台任务、聊天会话列表与受控 Deep Agents 持久记忆适配。
- 开始 Phase 11；不开放 Shell、任意文件工具、真实发布或用户级记忆，长期记忆仅为受审核的 Agent 运营偏好。
- 已修复 `card_script` 单字符串兼容问题；GitHub Trending 的 daily/weekly 项目会并行整理，前 5 项合成一个可回溯来源的草稿，草稿表新增 `evidence_json`。
- 已增加 Redis + ARQ Worker；聊天采集请求先返回“已加入后台队列”，前端轮询运行状态，不再同步等待所有 LLM 生成。
- 已增加聊天会话列表、显式新建与浏览器侧活动会话恢复；历史消息继续从 PostgreSQL 读取。
- `deepagents`、`langchain-openai` 和 PostgreSQL 检查点依赖已锁定；受控持久记忆适配仍待完成 Tool/命名空间验证，未开放默认文件或 Shell 能力。
- 已新增会话删除 API 与聊天列表删除按钮；删除前二次确认，当前会话删除后自动选择最近会话或创建空会话。专用空测试会话已创建并删除，验证其不会再出现在列表中。
- 已只读确认后台 GitHub 聚合任务的状态回写缺陷：Worker 完成/失败事件与路由创建事件使用同一序号，违反唯一约束；当前任务未产出新草稿，需在获得确认后修复事件序号并补充后台任务集成验证。
- 已完成事件序号修复：事件仓储统一自动分配连续序号，API 路由、同步 Agent 和 ARQ Worker 已移除固定序号。新增 Worker 成功/失败替身测试，离线测试 26 项通过；API 与 Worker 容器已重建。截图对应的历史任务已明确标为失败并追加审计说明，未重新采集。
- 已完成增强生成第一阶段：`0007_enhanced_generation` 增加选题规划、质检、结论引用和生成模式字段；增强模式默认关闭。新增受控会话附件读取 Tool、无 Shell/网络的登记文本脚本注册表及三类运行规则 Skill。28 项离线测试与前端构建通过。Deep Agents 实际会话适配和自然语言脚本执行入口待明确“登记脚本”或“用户自定义脚本”的执行模型后继续。
- 已接入自然语言登记脚本入口：当前会话附件可在明确“运行脚本 <ID>”指令下被读取，结果作为新的私有 Markdown 附件返回短时下载链接；未登记脚本与任意 Shell/Python 均不能执行。API 和前端容器已重建。Deep Agents 实际会话适配与长期记忆仍待完成。
- 2026-09-06：修复来源全失败仍显示“采集完成”的聊天状态问题。Worker 专项 3 项、全量离线 29 项、前端生产构建与容器健康检查均通过；未重跑 GitHub 历史采集任务，网络连接问题仍保留待单独处理。
- 2026-09-06：按确认向 app/worker 注入 `host.docker.internal:7895` 出站代理，服务仍健康。GitHub 请求通过代理后停在本地 TLS 根证书不受信任，未降低证书验证；待确认后以私有、未纳入 Git 的根证书文件构建系统 CA 包。
- 2026-09-06：已将有效 SteamTools 根证书以私有只读挂载方式接入 app/worker，入口脚本合并公共 CA 包后保留 TLS 验证。Worker 通过实际 `httpx` 请求 GitHub Trending 返回 200；未触发采集任务或写入业务数据。
- 2026-09-06：修复 GitHub 聚合草稿生成器依赖遗漏。专项测试 3 项、全量离线 30 项通过，Worker 重建并启动正常；历史聚合任务未自动重跑。
- 2026-09-06：GitHub 改为单项目深度草稿：候选采集后先查 `project_introductions` 与既有草稿，再对热度最高的一个未介绍项目读取 README。HN 增加受大小上限与并发限制的公开正文抽取；LLM 证据摘录和正文提示改为 450–650 字。`0008_project_introductions` 已迁移，32 项离线测试通过，app/worker 重建且 API 健康检查成功；未重跑历史采集、未调用真实 LLM 或发布。
- 2026-09-06：已将 GitHub 去重语义校正为“实际发布后才算已介绍”。`0009_manual_publication` 增加发布回填审计和草稿发布状态；审核页只在审核通过后显示回填链接入口。预发布的历史介绍记录不再影响筛选。后端离线测试 35 项、前端生产构建和容器迁移均通过；未执行真实平台发布。

## 2026-09-08｜发布素材库与纯文本预览阶段

- 用户已确认实施独立发布素材库、多图上传/选择、删除未引用素材、发布前预览和纯文本正文。
- 开始 Phase 15；聊天附件保持原状，预览和素材管理不调用真实公众号创建或发布接口。
- 首轮离线测试发现图片 MIME 与扩展名未做一一匹配，且纯文本规范化会剥离 HTML 标签；已定点修正校验并调整安全边界测试，不重复执行失败方案。
- 已完成独立 `publication_assets` 表与 `0011_publication_assets` 迁移；发布页支持一次上传多图、删除未被发布任务引用的素材、选择封面/至多六张正文插图及本地纯文本预览。聊天附件未迁移也未删除。
- `uv run pytest` 47 项通过；前端生产构建通过；app、worker、frontend 已重建。数据库迁移为 `0011_publication_assets (head)`，发布素材查询接口与前端入口均返回 HTTP 200。未上传测试图片、未创建公众号草稿或提交发布。
- README 已同步更新发布素材库、纯文本预览和受控公众号边界；Phase 14 与 Phase 15 均完成。
- 多图上传使用独立请求并汇总成功/失败数量，单张非法图片不会掩盖其他已成功私有保存的素材。
- 前端容器重启后的首次 HTTP 请求早于 Vite 就绪而提前结束；等待后复查返回 HTTP 200，app、worker 与公众号 MCP 均保持运行。
- 只读诊断发布素材准备失败：私有素材上传成功，应用调用第三方 MCP 的 SSE 入口返回 HTTP 502。未重试外部上传或创建草稿；后续需在确认后排查第三方服务启动/凭据状态，并将健康检查升级为实际 SSE 可用性检查。
- 首轮只修改 Compose 默认 `NO_PROXY` 未生效：容器实际值被用户已有 `OUTBOUND_NO_PROXY` 覆盖。现改为在 Compose 展开结果末尾无条件追加 Docker 内网服务名 `wechat-mcp`，不修改 `.env`。
- 已完成 Phase 16：公众号 MCP 构建过程成功应用 SSE 重复响应头兼容补丁，并重建单一 MCP 服务。app 内部 SSE 请求返回 HTTP 200、`text/event-stream`，socket 握手已收到 endpoint 事件；没有执行公众号 Tool、上传、草稿创建或发布。
- 已完成 Phase 17：项目自有 MCP 构建补丁增加 SSE 会话映射与 `POST /messages` 转发，解决 MCP 初始化的 HTTP 404。专项离线测试 4 项通过；内部 `endpoint → initialize` 验证返回 HTTP 202；app 和公众号 MCP 容器均已运行。未执行任何 `tools/call`、素材上传、草稿创建或发布。

## 2026-09-09｜生成记录、AI 审核与受控插图阶段

- 用户确认自动封面方案 A：自动审核通过且无封面时可调用已配置图片服务生成一张封面；自动化终点严格限制为创建公众号草稿，不提交发表。
- Phase 20 已开始：先建立数据审计、受控 Tool 与 Skill，再接入聊天、后台任务和前端；将以离线替身完成验收，不触发真实模型、图片服务或公众号调用。
- 首次部署发现 Alembic 版本列限制为 32 字符，超长迁移 ID 使 app 容器重启；PostgreSQL 事务已回滚，现缩短 ID 后重试迁移。
- 已完成 Phase 20：迁移 `0013_generated_images_reviews` 已应用，新增插图和自动审核审计表存在；app、worker、frontend 运行，草稿接口与前端均返回 200。62 项离线测试与前端生产构建通过。未调用真实 LLM、图片生成或公众号接口。

## 2026-09-09｜Agent 决策式自动配图阶段

- 用户确认“自动配图”由 Agent 决定数量和插入位置。新增独立勾选、`0014_auto_illustration_request` 迁移和受控配图规划 Tool：LLM 可用时返回最多 3 个段落位置；不可用时基于文章自然段数量作确定性、可审计降级。
- 后台顺序为生成文案 → 自动配图规划及私有保存 → 可选自动审核 → 通过后可选创建公众号草稿；没有自动发表路径。
- 63 项离线测试、前端构建通过；app、worker、frontend 容器运行，迁移已应用，草稿接口返回 200。未调用真实模型、图片服务或公众号接口。

## 2026-09-09｜受控附件工作区与生成过程状态阶段

- 用户确认：附件须同时保存在 MinIO 私有对象存储与 Agent 受控工作区；只有明确引用附件时才作为上下文传入。同时确认生成记录需显示文字与图片的运行状态。
- Phase 22 已开始。实施边界：不读取历史附件，不在验证中调用真实模型、图片服务或公众号；不会展示模型原始推理。
- 前端文件存在历史单行 JSX。尝试使用 Prettier 机械格式化时发现本地未安装该依赖，npm 访问缓存目录被拒绝；未写入文件、未下载依赖，后续改用定点补丁。
- 第二次使用本机离线 Prettier 时，Windows 拒绝写入正在使用的前端源码；未产生文件改动。改为在应用壳层挂载独立“正在生成”状态组件，避免重写历史单行 JSX。
- Python 编译通过。首次离线测试发现 Worker 异常发生在文字阶段事件之前，测试误期望“文字生成失败”；已确认生产逻辑的“执行失败”事件正确，将仅修正断言后重试。
- 修正后 65 项离线测试全部通过。前端构建首次在项目根目录执行，因该目录无 `package.json` 未开始构建；将切换至 `frontend/` 目录重试。
- 前端生产构建通过；Docker 已创建 `agent_workspace_data` 并挂载至 app/worker，API 与前端健康检查均为 HTTP 200。首次工作区写权限探针的 PowerShell 转义错误，未产生探针文件，将使用固定路径重试。
- 固定路径探针确认命名卷初始归 root，`appuser` 无法写入。将新增一次性 `workspace-init` 容器，仅初始化该专用卷的 UID/GID 所有权，随后复测 app 的临时写入能力。
- 初始化容器首次启动失败，日志显示 `mkdir: missing operand`。原因是 Compose 未将完整脚本传给 `sh -c`；改为 entrypoint 内嵌完整脚本后再重建。旧 app/worker 保持运行，未替换为失败版本。
- Phase 22 已完成。新附件写入 MinIO 私有对象与会话隔离工作区；历史附件只会在明确授权使用时补建副本。明确图片指令可将当前选中图片复制为草稿独立私有素材并绑定封面或正文段落，未上传公众号。后台采集新增文字、配图规划、逐图生成、审核与草稿投递阶段事件；生成记录页面在有运行中任务时自动刷新分项状态。
- 验证：Python 编译通过；65 项离线测试通过；前端生产构建通过；Docker Compose 配置通过；`workspace-init` 已完成专用卷权限初始化，appuser 临时写入/删除探针成功；`/api/chat/agent-runs/active`、API 文档与前端均返回 HTTP 200。未创建真实附件、草稿、图片或发表记录，未调用模型、图片服务或公众号接口。

## 2026-09-09｜生成队列详情、审核改稿重试与术语解释阶段

- 用户确认将运行中任务并入生成队列，点击后在右侧查看文字、配图与审核状态；同时确认自动审核未通过后最多两次“按意见改稿→复审”，以及正文保留原文术语并在括号内翻译或解释。
- 只读诊断确认：任务 `0c91afdc-dd87-4d6a-8846-21f8afcc2b07` 已写入“文字生成完成”和“自动审核中/完成”事件；自动审核规则通过（975 字、6 段），模型审核以术语解释不足和绝对化措辞待核实为由拒绝。`failed` 是内容审核未通过，不是基础设施错误。
- Phase 23 开始。边界：最多额外两次改稿；仅内容审核拒绝触发改稿，不对网络或模型运行异常盲目重试；来源字段锁定；失败后不创建公众号草稿，更不发表。
- 首次运行 uv 静态编译与离线测试被受限环境拒绝访问 Python 解释器（os error 5），尚未执行测试；将改用已授权项目 uv 环境重试，不改变依赖或配置。
- 已授权的 uv 编译和 65 项离线测试通过。前端首次构建暴露两项问题：受限环境不能写入 TypeScript 增量构建缓存，且生成阶段元组被 TypeScript 推断为字符串/对象联合类型；已定点收窄阶段类型，随后将用授权环境重新构建。
- 已授权前端生产构建通过。新增自动改稿边界测试后，离线测试共 67 项通过；其中覆盖“初审失败、两次改稿、第三次审核仍失败即停止”以及审核意见汇总。尚未部署或调用外部服务。
- Docker Compose 配置校验通过；app、worker、frontend 已重建。应用启动日志确认 Alembic 已升级 `0014_auto_illustration_request` 至 `0015_draft_revision_history`；app/worker 运行，API 与前端 HTTP 200，`/api/chat/agent-runs/active` 返回空数组，OpenAPI 已注册草稿版本历史接口。未触发采集、模型、生图、公众号草稿或发表调用。
- Phase 23 完成：运行任务已进入生成记录队列，右侧可查看受控阶段详情；草稿详情可显示封面、插图、审核意见和自动改稿版本。自动审核拒绝会最多改稿两轮后停止，来源字段和原文标题尾注保持服务端锁定。

## 2026-09-09｜聊天页初始化卡住诊断

- 只读检查：本机聊天会话列表返回 HTTP 200，浏览器等价 CORS 预检返回 HTTP 200；应用日志显示同一浏览器会话已成功读取会话详情。无 app/frontend 容器异常或接口失败日志。
- 结论：截图状态由前端 `conversation` 尚未加载时的单一 loading 分支呈现；当前无法复现。若用户确认，下一步仅改善前端初始化失败、超时与重试可见性，避免任何暂时请求中断被无限显示为“正在创建对话”。

## 2026-09-09｜当前后台任务状态检查

- 只读检查确认当前 API 仍列出任务 `66f87883-2e9d-4cfc-9559-a9e7764e6ab6` 为运行中：文字阶段“文字生成中”，配图和自动审核尚未开始。
- Worker 已在 ARQ 300 秒超时后取消该任务；取消发生在 LangGraph 持久化阶段。现有 Worker 仅捕获 `Exception`，未覆盖 Python 3.12 的取消异常，导致聊天任务没有回写失败终态。不能对该任务直接重试，避免超时后残余持久化造成重复内容。

## 2026-09-09｜独立图片后台任务与发布状态队列阶段

- 用户确认：慢速生图转为独立后台任务，图片任务按 ID 轮询，并为请求/任务设置更长超时；发布情况按生成记录方式列出文章、按时间筛选并显示状态。
- 新增 `image_generation_jobs` 与迁移 `0016_image_generation_jobs`。图片请求超时默认为 900 秒，采集/收束任务默认为 600 秒，单图 ARQ 任务默认为 1200 秒；这些值可通过 `.env` 覆盖。
- 采集 Worker 只负责文字生成和创建图片任务；单图 Worker 写入独立状态/事件，所有图片终态后由幂等收束任务执行自动审核。取消超时会回写“执行超时”，不再留在活动队列。
- 自动审核需要封面时先排队生成私有封面；投递层不再同步调用图片模型。最终边界保持为“最多创建公众号草稿，不提交发表”。
- 离线测试 68 项通过；Python 静态编译、前端生产构建与 Compose 配置校验通过；容器重建输出确认 Alembic 迁移头为 `0016_image_generation_jobs`。容器重建后补充的发布列表与逐任务轮询已通过 TypeScript 静态检查，仍需重建前端容器；后续最终日志/HTTP 复查被当前受限执行权限拒绝，未绕过限制。

## 2026-09-09｜生成失败可见性与文章级发布素材阶段

- 已完成 Phase 25：失败/超时的采集任务进入生成记录队列；当候选全部在模型生成阶段失败时会写入失败终态，最终消息使用最后一条文字阶段事件，不再误显示“文字生成中”。
- LLM 返回余额不足、限流、超时、连接或 5xx 时，生成器会以相同来源证据生成 `deterministic_fallback` 草稿并写入风险说明；模型输出结构或事实校验失败仍保留失败，避免掩盖内容质量问题。
- 已新增 `0017_draft_publication_assets`，发布素材改为文章级显式绑定；当前文章只显示自己上传或 Agent 生成的素材，未修改任何历史素材或配置。
- 公众号 MCP 远端列表错误现在写入安全分类和返回码（若有），用于后续准确区分 MCP HTTP、网络、凭据和微信授权；前端不显示第三方原始错误、请求或密钥。
- 验证：71 项离线测试通过，Python 编译、前端生产构建与差异格式检查通过；尚待容器迁移/HTTP 复查，不调用真实模型、图片或公众号接口。
- 容器复查完成：Alembic 已升级至 `0017_draft_publication_assets (head)`；app、worker、frontend 均已重建并运行，健康接口和生成记录接口返回 200，前端 HTTP 200。已确认文章级素材查询仅返回指定草稿的 1 项关联素材。历史 `d649facd-077d-47eb-84b8-7df09b2185cb` 已定点修正为 failed，未重试或触发外部调用。

## 2026-09-09｜全局失败通知中心

- 已新增 `0018_notifications` 迁移和通知 API；聊天生成、图片任务及公众号同步/投递失败都会写入统一通知中心。
- 已在全局右上角加入铃铛、未读红点、详情列表、跳转、单条已读/删除和全部已读操作；通知删除不影响底层审计。
- 验证完成：76 项离线测试、前端构建、Compose 重建、`0018_notifications` 迁移及本地 `/api/notifications` 返回均通过；未调用真实模型、生图或公众号接口。

## 2026-09-09｜图片回退适配与记录列表精简

- 图片服务最小化请求已验证可用；适配器现会忽略空/无效 Base64 并下载 HTTPS URL，回归测试覆盖该响应形态。
- 生成队列和发布文章列表均限制最大可视高度并提供滚动；失败任务按日期并入生成记录，发布页改由左侧文章列表单一选择文案。
- 验证完成：78 项离线测试、前端生产构建、app/worker/frontend 重建，应用文档与前端 HTTP 均返回 200。未执行修复后的真实图片生成，不增加额外额度消耗。

## 2026-09-09｜废弃草稿重新生成

- 已开始 Phase 28：移除 `content_drafts.source_item_id` 的一对一模型约束，并将采集去重迁移为按草稿状态判断。
- 首轮离线测试发现 `DraftRow.source_item` 仍引用旧的 `SourceItemRow.draft` 关系名，导致 SQLAlchemy 映射初始化失败；已定点改为 `drafts`，尚待重跑测试和应用数据库迁移。
- 修复后离线测试 80 项通过，静态编译通过；容器迁移已升级至 `0019_reusable_source_drafts (head)`。数据库确认旧的一对一唯一约束已移除，app 文档页与前端均返回 HTTP 200。

## 2026-09-09｜Worker 图片存储与配图失败审核边界

- 已定位截图中的三个图片任务：供应商请求与 URL 下载成功，MinIO 保存因 Worker 容器错误使用 `localhost:9000` 失败。
- 已为 Worker 注入 Docker 内网 `MINIO_*` 配置，并在图片任务失败/超时时跳过自动审核批准和公众号草稿投递，改写为“自动审核待人工处理”。
- 首次只读 MinIO 验证误用了已不存在的 `_client` 属性，未产生网络写入；改用 `client` 后确认 Worker 可访问 `minio:9000` 的私有桶。81 项离线测试、静态编译和 Compose 配置校验通过；app/worker 已重建，应用与前端 HTTP 均为 200。未重试历史图片任务，未调用图片或公众号服务。

## 2026-09-09｜自动配图与插图定位 Skill

- 新增 `app/skills/auto-illustration/SKILL.md`，明确“位置规划 → 图片后台任务 → 单图生成 → 私有保存并按段落绑定 → 收束状态”的受控执行顺序。
- 单图生成 Skill 已收敛为该工作流的子步骤；自动配图不读取聊天附件、不写入纯文本正文、不上传公众号。
- 新增离线回归测试覆盖先规划后按位置生成的顺序；82 项离线测试、静态编译和 Compose 配置校验通过。未调用真实图片、MinIO 或公众号服务，无需重启容器。

## 2026-09-09｜Phase 31 开始

- 已取得实施确认：采用实际 `create_deep_agent` 接入 PostgreSQL 会话 checkpoint 与结构化业务记忆；重组项目 Skill、受控 Tool 和 MCP 适配层。
- 同步处理：配图任一失败或超时即中断自动审核/公众号草稿投递；图片提示词禁止文字；发布页三步操作按钮改为更紧凑布局。
- 已完成本地依赖与现有会话路径只读核对，尚未调用真实模型、图片、搜索或公众号服务。

## 2026-09-09｜Phase 31 完成

- 新增迁移 `0020_chat_session_memory`、受控会话 Tool、DeepAgent 运行时与 3 个会话 Skill；`.env` 和 `.env.example` 已启用 `DEEP_AGENT_ENABLED=true`，仍需现有 LLM 配置有效才会实际调用模型。
- 图片失败/超时会中断自动审核并保留文字草稿；图片提示词改为仅生成无文字的物品、结构或场景；发布页三步操作在常规宽度横向等宽排列，窄屏自动纵向排列。
- 验证：Python 编译、83 项离线测试、差异格式检查、容器重建、`0020_chat_session_memory` 迁移和本地 app/前端 HTTP 检查均通过。未调用真实大模型、生图、Exa、MinIO 写入或公众号接口。
- 受限环境首次运行 uv 因缓存目录存在冲突未开始测试；使用同一命令的已授权项目 uv 环境重试后通过。首次 Docker 操作因受限环境无法访问本机管道未开始；授权后完成重建与只读状态检查。

## 2026-09-09｜Phase 32 开始

- 用户已确认：README 仅传入受限纯文本；修复 Compose 中 DeepAgent 开关注入；复合采集与自动配图走后台任务；以本地 OCR 作为无文字质量门，失败最多重试两次并阻断自动审核。
- 诊断期间一次读取命令误引用不存在的 `tests/test_github.py`，未修改文件；后续改为从 `tests/test_sources.py` 定点读取相关测试。

## 2026-09-09｜Phase 32 代码完成

- 已完成 README Base64 正文清洗、DeepAgent Compose 注入、复合采集自动配图参数、无文字图片 OCR 质量门及对应 Skill/测试更新。
- 首次大补丁的路由锚点未匹配，但此前已应用的独立变更无重复；随后按小范围上下文完成剩余路由补丁。
- 受限环境的 uv 缓存初始化返回 `os error 183`；改用项目现有 `.venv` 运行 18 项相关离线测试，全部通过，`git diff --check` 通过。
- Docker 前台构建被执行窗口中断，且重复构建导致 Docker 响应占用；已仅停止本次产生的构建进程，改为单一隐藏后台构建并保留日志。构建尚在下载运行 OCR 所需的 Debian 图形库，旧 app/worker 容器暂未替换。

## 2026-09-10｜Phase 33 开始

- 用户已确认：重生成必须复用原生成记录，且需查明自动改稿模型不可用原因。
- 已定位普通采集分支无条件新建 `ChatAgentRunRow`；自动改稿和自动审核均会把底层异常压缩为通用文案，尚未实施修改。
- 已从私有运行日志确认最近三次自动改稿均为 `APITimeoutError`；容器内 LLM 已启用且密钥、模型名存在，当前请求超时为 20 秒。后续会将该结果安全映射为“模型请求超时”，不输出密钥或供应商原始响应。
- 读取迁移目录时递归枚举触及 `.pytest_cache` 并返回访问拒绝；未写入或删除任何缓存，后续改为定点读取 `migrations/versions`。
- 已实现原记录内重生成、原草稿版本递增和模型失败安全分类，并新增两项专项断言。首次专项测试有 1 项 Worker 既有失败路径测试未进入替身仓储；正在检查测试替身与新增 Worker 导入的交互，未将其归因于业务逻辑。
- 已定位并修复 Worker 异常处理缩进：普通采集的通用 `except Exception` 被首次补丁错误置于重生成函数末尾，导致测试替身无法记录失败；恢复后 28 项专项测试、Python 编译和差异格式检查全部通过。
- Docker 重建 app/worker 成功；容器确认 `DEEP_AGENT_ENABLED=true`、Worker 已加载 `process_draft_regeneration_job`，`/docs` 返回 HTTP 200。未调用模型、图片、Exa、MinIO 写入或公众号接口。
# 2026-09-10｜Phase 34

- 已新增 `CONTENT_LLM_TIMEOUT_SECONDS=90` 与 `CONTENT_LLM_MAX_RETRIES=0`，并注入 app/worker。
- 正文生成、自动改稿、自动审核和配图规划已切换至独立模型设置；普通聊天与来源抓取仍使用 20 秒通用时限。
- 已完成 uv 环境下的 28 项专项测试、Python 编译、差异格式检查；app 与 Worker 运行时均确认使用 20 秒聊天/HTTP 与 90 秒长文模型时限。本阶段完成。
# 2026-09-10｜Phase 35

- 采集零新增结果现会记录去重、跳过或无候选原因到当前会话摘要，并保留已有活动草稿。
- `get_current_conversation_context` 现返回当前会话最近 8 条受限消息；DeepAgent 系统规则要求对任务结果追问先调用该 Tool。
- 19 项上下文与 Worker 专项测试、Python 编译和差异格式检查通过；Worker 已重建并正常启动。app 与 frontend 当前均已运行。

# 2026-09-10｜Phase 36

- 已完成普通聊天的即时回显与后台会话回复：前端先显示本地发送消息，API 立即提交用户消息并入队，Worker 使用 DeepAgent 生成回复后写回同一会话。
- 新增 `CONVERSATION_AGENT_TIMEOUT_SECONDS=45`；模型超时、未配置或异常会写入失败事件和可见回复，用户消息与上下文不会丢失。
- 验证：Python 编译、20 项会话/Worker 专项测试与前端生产构建均通过。尚未重启容器或调用真实模型。

# 2026-09-10｜Phase 37

- 已完成四部分内部正文结构：背景与切入、技术与过程、价值与边界、后续观察均不作为可见标题；每部分可含一到两个自然段。
- 正文和自动改稿均输出并校验 `body_sections`，再由服务端合并、缩进并追加唯一原文标题尾注。自动审核总长度改为 800–3200、段落数改为 4–8。
- 已将长文模型时限提高至 120 秒并统一用于正文、自动改稿与审核；新增规则、部分字数与生成完成日志。29 项专项测试和 Python 编译通过，待容器重建验证。

# 2026-09-10｜Phase 38

- 用户已确认：对话从初始化开始由会话级 DeepAgent 管理，而非先走独立关键词/意图分类再调用 DeepAgent。
- 已完成只读调用链核对：路由层的 `build_conversation_model().decide()` 是第一段模型调用；通用对话会排入 `process_general_chat_job`，由 `ContentDeepAgent.respond()` 触发第二段模型调用。
- 已记录真实失败链：第一段模型 HTTP 200 后结构校验失败；第二段 DeepAgent 多轮调用后收到 HTTP 429，界面却显示为超时。
- 首次以 `uv run` 读取第三方库签名受环境拒绝（os error 5）；未重试同一路径，将改用项目虚拟环境进行只读源码检查。
- 项目 `.venv` 同样由 uv trampoline 拦截，无法启动 Python 子进程（os error 5）；已改为直接读取已安装的 `deepagents/graph.py`，确认 `create_deep_agent` 原生支持 `response_format`、PostgreSQL checkpoint 和以自定义 `FilesystemMiddleware(tools=[])` 移除默认文件工具。
- Anaconda 环境仅完成语法编译；运行专项测试时缺少 `langchain_core` 与 `arq`，且既有 `.pytest_cache` 不可写。未安装依赖、未删除缓存；后续改用 Docker 应用镜像进行隔离测试。
- 容器导入、模型安全降级与 `/docs` 检查通过；但进一步核对镜像构建规则发现 `agent_skills/` 未被复制，实际 DeepAgent 首次读取 Skill 会失败。已将该目录纳入 Dockerfile，待最终重建后确认。
- 最终重建请求被 Codex 运行额度限制拒绝，未执行 Docker 命令、未替换当前运行中的 app/worker。已完成 Python 语法编译、差异空白检查，并核对 Dockerfile、DeepAgent 结构化响应和路由日志锚点；容器内 Skill 加载验证仍待额度恢复后执行。
- 用户在获知限制后明确授权重试。app 与 worker 已成功重建；容器内 `_agent_files()` 确认存在 `conversation-memory` Skill，限流安全降级断言通过，`/docs` 返回 HTTP 200，服务日志正常启动。未提交聊天消息、未调用真实模型、图片、采集或发布服务。

# 2026-09-10｜Phase 39

- 已新增 `CONVERSATION_LLM_MODEL`、`CONTENT_LLM_MODEL`、`REVIEW_LLM_MODEL`、`ILLUSTRATION_PLANNER_LLM_MODEL`；四项为空时均回退 `LLM_MODEL`，不需要修改既有 `.env`。
- 会话 DeepAgent、正文/自动改稿、自动审核和插图位置规划已按对应类别选模；启动/调用日志会记录类别和模型名，不记录用户正文、提示词或密钥。
- 已补充模型路由离线测试，待完成静态和容器运行态验证；不会为此发起真实模型请求。
- 已通过 Python 静态编译、差异格式检查、Compose 配置校验，以及新 app/worker 容器内的回退/覆盖模型路由断言；`/docs` 返回 HTTP 200，启动日志无异常。未调用真实模型、图片、Exa 或公众号接口。

# 2026-09-10｜Phase 40

- 已新增会话、正文、审核和插图规划四项独立 OpenAI-compatible Base URL；留空时保持统一 `OPENAI_BASE_URL` 行为。
- app/worker 已重建；模拟回退与独立覆盖断言、静态编译、Compose 校验和 `/docs` 存活检查均通过。未调用真实模型或外部接口。

# 2026-09-10｜Phase 41

- 已新增四项任务级 API Key，并按模型类型将模型、Base URL、API Key 重新整理到 `.env` 与 `.env.example`；统一配置仍是空字段的兼容回退。
- app/worker 已重建；静态编译、差异格式检查、Compose 校验、容器内密钥选择断言及 `/docs` 存活检查通过。未读取、输出或调用真实密钥。

# 2026-09-10｜Phase 42

- 会话 DeepAgent 现优先使用原生 Pydantic `structured_response`；缺失时仅接受最终助手消息中的完整 JSON，并以 `ConversationDecision` 校验。
- 已新增助手 JSON 成功、自由文本拒绝、用户 JSON 不可解析为决策的回归测试；app/worker 已重建，容器断言和 `/docs` 检查通过。未调用真实模型或业务接口。

# 2026-09-10｜工具式会话决策修复完成

- 已将会话 Agent 从网关原生 JSON Schema 模式改为 `ToolStrategy(ConversationDecision)`；模型须以结构化工具提交 Pydantic 决策。
- 已移除与当前 Deep Agents 不兼容的空 `FilesystemMiddleware`；文件工具继续由安全 Profile 排除。
- app/worker 已重建；容器内装配断言、真实 DeepAgent 图创建探测和本地 `/docs` 存活检查通过，未调用真实模型、采集、图片或发布服务。

# 2026-09-10｜README 草稿期私有快照

- 已新增 `draft_source_snapshots` 表与 0021 迁移，及 `DraftSourceSnapshotStore` 私有对象生命周期服务。
- 已接入初次 GitHub 草稿生成、原记录重生成、人工审核通过和自动审核通过路径；未读取 `.env`、未调用 GitHub、模型或公众号接口。
- 待执行：容器重建使迁移生效，并以替身对象验证保存、复用和删除行为。

- 已完成：app/worker 重建后迁移版本为 `0021_draft_source_snapshots`，新表字段已核对；以临时容器内存替身运行保存、复用、审核删除回归用例通过；本地 `/docs` 返回 200。语法检查使用 `/tmp` 字节码缓存通过，未调用真实模型、GitHub、MinIO 或公众号接口。

# 2026-09-10｜GitHub 候选与发布页面只读核对

- 使用 planning-with-files Skill 记录本轮多文件规划与只读结果。
- 已确认数据库排除名单为 0 条，未执行删除；等待用户确认后才会变更前端信息架构与 GitHub 前置去重逻辑。

# 2026-09-10｜Phase 46 已确认

- 用户确认：为遗留运行记录增加安全收束、默认启用自动配图/自动审核、优化视觉并在会话切换时定位最新消息。
- 安全边界：只更新超过安全时限的 `running` 审计为失败，不删除草稿、图片、会话或历史消息；不调用外部服务。
- 已完成：默认启用两个自动化选项，使用有选中状态的图标卡片；消息列表在会话或消息数变化后定位底部。新增遗留运行收束回归测试通过，Python 语法检查与前端 TypeScript 检查通过。

# 2026-09-10｜Phase 47 已确认

- 用户确认：收束运行 ID `d284a710-ba08-454b-ab0f-0b6e587b1f7b`，并增加仅删除生成任务审计的队列删除能力。
- 已核对：关联草稿 `0905c195-2d70-4b87-b256-ccf859a56638` 仍为待审核，5 个图片任务均已完成；删除功能不得删除这些业务数据。

# 2026-09-10｜Phase 47 已完成

- 已重建 app、worker 和 frontend 容器，并通过生成记录接口触发遗留运行收束。
- 指定运行已变为 `failed`，写入完成时间、失败说明和“遗留生成任务已收束”审计事件；关联草稿仍为待审核，插图仍存在。
- 已增加结束态生成记录的删除接口和前端“删除当前生成记录”入口；运行中记录不能删除，业务草稿及其内容不会被删除。

# 2026-09-10｜Phase 48 已部署，等待私有配置更新

- 内容模型超时、连接或可恢复的服务端故障将不再降级为模板正文，而是结束原记录并保留安全失败原因；原草稿及历史版本不被覆盖。
- 失败生成记录可使用“重试生成”，优先在原运行记录内基于已保存来源重生成已有草稿；没有草稿时复用原来源范围重新采集，不创建第二条运行记录。
- 默认值、Compose 回退值和示例配置已更新为正文模型 600 秒、文字任务 900 秒。当前私有 `.env` 仍显式配置 120/600，等待用户自行更新后再复核。

# 2026-09-11｜Phase 49 已完成只读状态核对

- 已确认服务容器均在运行，当前无运行中任务。
- 最近流程未完成于公众号草稿箱：创建草稿 MCP 返回 HTTP 502，草稿与生成图片仍保留，未发生真实发布或去重写入。
- 已记录三个待确认改动：模型决定投递素材、一次性评分审核、草稿箱创建即终态并去重。

# 2026-09-11｜Phase 49 已确认并开始实施

- 用户确认采用建议：四类来源在公众号草稿创建成功后去重，自动审核阈值为 85 分。
- 本阶段不会调用真实模型、图片、微信公众号接口或读取真实 `.env`；仅进行本地代码、迁移和替身验证。

# 2026-09-11｜Phase 49 后端与前端首轮完成

- 已新增 85 分自动审核阈值、评分/严重度输出和一次自动改稿上限。
- 已将公众号草稿创建成功设为 `draftbox_created`，并在获得远端草稿标识后写入来源去重记录。
- 已将发布页替换为草稿箱投递视图；提交发表与发布状态查询接口均返回不可用，不会触发微信发表调用。
- 待完成：Python、前端类型、专项替身测试、Compose/迁移和容器运行态验证。

# 2026-09-11｜Phase 49 验证环境记录

- Python 语法编译通过。
- 宿主专项 pytest 因缺少 `minio`、`psycopg` 在收集阶段停止，未执行任何业务断言。
- 根目录运行 `npx tsc` 会触发 npm 临时下载并因用户 npm 缓存目录权限失败；后续只使用前端已安装依赖的本地二进制，不安装依赖。

# 2026-09-11｜Phase 49 已完成

- 发布素材准备接口已改为不接收人工封面或插图选择；由已配置的素材规划模型在既有生成图中选择一张封面和零至六张正文插图，选择失败会明确保留在可处理状态。
- 自动审核会一次返回所有已识别缺陷、严重度、0 至 100 分评分与 85 分阈值；无严重问题且达到阈值即通过，自动改稿最多一轮。
- 微信公众号草稿创建成功即进入 `draftbox_created` 终态，并以四类来源的源条目写入去重记录；提交发表和发布状态刷新端点固定返回不可用，避免个人账号误触发表。
- 应用镜像内后端替身检查、前端类型检查、前端生产构建、OpenAPI 无人工素材请求体验证、草稿发表端点 410 验证、应用文档与前端 HTTP 检查均通过；Compose 服务保持运行。

# 2026-09-11｜Phase 50 已完成

- 已定位公众号草稿上传失败为微信接口 40164：当前出口 IP 未在白名单。网络到达微信，MCP 服务健康；未读取真实凭据或发起重试。
- 已把该错误改为可操作的安全提示，并仅显示在草稿箱投递区域；自动审核区域不再承载投递失败。
- 已增加完整图文草稿预览：封面、正文段落和按实际位置插入的已选正文图。
- 已为公众号 MCP 单独配置运行时代理变量，默认直连；容器内 HTTP/HTTPS/ALL 代理变量均为空。
- Python 编译、前端类型检查、前端容器生产构建、白名单错误替身检查和前端 HTTP 检查通过。应用镜像没有 pytest，专项 pytest 未启动，未安装额外依赖。

# 2026-09-11｜Phase 51 已完成

- 已将 Agnes Image 2.5 Flash 切为项目默认图片模型，并在 image-generation Skill 的 references 中固化端点、`extra_body.response_format`、尺寸与无文字图片约束。
- 已实现：常用术语不再被强制解释、预览解除固定高度、系统生成图片在启用自动配图的重生成中替换、插图提示词有稳定差异方向、草稿箱重投接口和页面入口、以及按 `attempt_started_at` 隔离的图片收束。
- 若文案重生成前存在未成功的投递记录，会清空其旧素材选择并在下一次投递时使用新图片；已成功入草稿箱的记录不会被改变。
- Python 编译、前端 TypeScript 与生产构建通过；app、worker、frontend 入口均通过 HTTP 200。容器内装配断言确认术语策略、差异化视觉方向和草稿箱重投路由可用。
- 指定运行 `c2c2208b-a834-4422-adb6-6be506999a34` 已从旧超时误判修复为 `completed`，摘要为“文字与配图完成，当前草稿待审核”；未触发模型、审核、图片或公众号调用。
- 私有 `.env` 仍显式覆盖为 `agnes-image-2.1-flash`。为不覆盖用户配置，运行时切换需要用户自行将该行改为 `IMAGE_GENERATION_MODEL=agnes-image-2.5-flash` 后重启 app 与 worker。
# 2026-09-11｜Phase 54 开始

- 用户已确认将文案生成与审核整合为受限子 Agent。将保持会话 DeepAgent 的持久 checkpoint 边界不变；生成/审核子 Agent 不持久化记忆，也不获得副作用 Tool。
- 本阶段仅改造本地代码与测试；不会读取私有 `.env` 或调用真实模型、联网搜索、图片、MinIO 写入或微信公众号接口。

## 2026-09-11｜Phase 54 子 Agent 初步实现

- 新增 `app/agents/content_task_agents.py`：文案与审核各用一次性、无工具/无记忆/无 checkpoint 的 `create_deep_agent`，输出分别受 Pydantic 模型约束。
- 基线和增强文案的最终写作已接入文案子 Agent；增强模式原有的检索规划、选题规划与质检仍保持轻量受控调用。审核模型调用已接入审核子 Agent；硬规则、通过阈值与工作流状态机未下放。
- 待完成：补充子 Agent 装配与调用链回归测试，运行静态检查和容器验证。

## 2026-09-11｜Phase 54 验证记录

- app/worker 已重建；文案子 Agent 的容器替身断言通过，确认其装配参数为零 Tool、零 Skill、零 Memory、零 Subagent。
- 首次审核替身断言失败：测试替身的 Python `__init__` lambda 意外返回了记录值，触发 `TypeError`，未进入应用实际审核逻辑；将改为返回 `None` 的替身后再验证。
- 第二次审核替身已进入应用逻辑，但断言预期的审核意见展示文本与现有规范化函数输出不一致；未修改业务代码，将先读取脱敏后的实际结构后更新断言。

## 2026-09-11｜Phase 54 完成

- 修正审核展示文本断言后，容器内文案与审核替身断言均通过：文案 Agent 为零 Tool/Skill/Memory/Subagent；审核 Agent 的结构化结论仍须经过本地固定规则、评分阈值和反馈规范化。
- Python 静态编译通过；app/worker 已重建并正常启动，`/docs` 返回 HTTP 200，Worker 注册 6 个后台函数且启动日志无异常。
- 新增 `tests/test_content_task_agents.py` 并更新增强生成测试；容器镜像未安装 pytest，故未执行 pytest 收集。未读取真实 `.env`，也未调用真实模型、搜索、图片或微信公众号接口。

# 2026-09-11｜Phase 55 开始

- 用户已确认：微信公众号功能不再调用 MCP，应用改为执行项目内 `wechat-official-account` Skill 的本地官方 API 脚本。
- 实施边界：仅素材上传、草稿箱创建和只读草稿箱读取；不调用发布或已发表文章读取接口，不读取或改写私有 `.env`，不发起真实草稿创建。

## 2026-09-11｜Phase 55 完成

- 已删除公众号 MCP/SSE 的应用调用、Compose 服务与构建脚本；app/worker 改为经 `WechatOfficialAccountTool` 装载本地 Skill 脚本并直接请求官方 REST API。
- 公众号接口显式不使用 `OUTBOUND_*` 代理；应用不再对正文插图作 1 MB 本地拒绝，微信官方接口响应才是最终裁决。
- app/worker 已重建，MCP 容器已作为 Compose orphan 移除而命名卷未删除。Compose 列表不含 MCP，`/docs` 为 HTTP 200；占位凭据装载与无网络替身投递验证通过。
- 宿主 pytest 缺 FastAPI，应用镜像也不含 pytest；未安装依赖，已完成 Python 静态编译和容器内替身验证。

## 2026-09-12｜结构化输出双模式与一致性整理

- 用户确认诊断并选定方案 3：保留 Pydantic 校验，通过配置在 `ToolStrategy` 与“提示词 JSON + 本地校验”之间切换。原因已定位为思考模式模型拒绝 `tool_choice: required`（HTTP 400），与提示词或素材无关。
- 已新增 `LLM_STRUCTURED_OUTPUT_MODE` 与三项任务级覆盖；留空回退统一开关、再回退 `tool`，旧配置行为不变。`json` 模式不传 `response_format`，并按模式注入互斥的系统提示（`tool` 要求调用结构化工具，`json` 要求直接输出 JSON 且不得调用该工具），避免两套指令同时出现。
- 已按当前模型分工设置：内容与审核为 `json`（两者均为思考模式模型），会话保持 `tool`。校验失败一律明确报错，不降级生成替代内容、不执行业务操作。
- 已实施此前建议的前 4 条：修复 11 项滞后测试、同步 `.env`、删除前端 394 行死代码与失效 API 方法、更新 README。
- 验证：后端完整套件 121 项通过、0 失败（2 项 `tmp_path` 用例因沙箱限制改用等价手工脚本验证通过）；前端 `tsc` 0 错误、生产构建 1580 模块成功。
- 环境限制记录：容器运行时会占用 `runtime_logs/news-agent.log`，需把 `LOG_FILE` 指向临时路径再跑 pytest；`vite build` 需要更宽权限才能越过沙箱的管道限制。验证产生的 `frontend/.build-check/`、`.pytest-tmp/` 因沙箱拒绝删除而保留，可手工清理。
