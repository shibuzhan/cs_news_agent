# 项目协作约定

## 当前项目目标

本项目是“资讯运营 Agent”。第一版从 arXiv、GitHub、Hacker News 和人工配置的官方 RSS 采集科技资讯，经清洗、去重、分类、热度排序和中文内容生成后，形成可人工审核、可复制发布的内容草稿。

发布平台暂不绑定。第一版不自动发布，不接入平台账号，不模拟登录，不绕过验证码或平台限制。

## 实施前确认规则

1. 每次准备修改业务代码、配置、依赖或数据库结构前，必须先向用户说明：
   - 准备实现的目标；
   - 涉及的准确文件或目录；
   - 具体实现方式；
   - 验证方法；
   - 安全边界和明确不做的内容。
2. 等待用户明确回复“确认”后再实施。
3. 需求、字段、技术选择或行为边界不清楚时，先询问用户，不自行扩大范围。
4. 用户仅要求解释、评估、诊断或询问可行性时，不修改业务代码。

## process.md 记录规则

- `process.md` 是追加式需求与变更记录，不删除或覆盖已有记录。
- 每次用户提出新要求、修改范围、确认、撤销或取消，都追加一条记录。
- 使用连续变更编号，并保持以下格式：

```markdown
<!--

### YYYY-MM-DD｜变更编号

- 用户指令：
- 影响范围：
- 处理结论：
- 授权状态：已确认 / 待确认 / 已撤销
-->
```

- 若新需求替代旧需求，在新记录中说明替代关系，不回写或删除历史记录。
- 未获得实施确认时，授权状态必须为“待确认”。

## 第一版功能边界

- `app/sources/`：实现 arXiv、GitHub、Hacker News、官方 RSS 四类采集器，并统一输出来源类型、标题、原文链接、发布时间、作者、摘要和原始标识。
- `app/services/`：负责文本清洗、重复检测、主题分类、热度排序、中文摘要和平台无关的内容草稿生成。
- `app/workflows/`：在确有人工暂停、恢复、条件分支和节点重试需求时使用 LangGraph，管理“采集 → 筛选 → 生成 → 人工审核 → 待发布”的显式状态。
- `app/api/`：提供触发采集、查询资讯、查看和编辑草稿、批准或驳回草稿的 FastAPI 接口。
- `app/storage/`：第一版使用 Docker 容器部署的 PostgreSQL 16（pgvector 镜像），保存原始来源、标准化资讯、处理结果、审核记录和可追溯链接；使用 Alembic 管理数据库结构。
- `tests/`：使用固定样例测试标准化、去重、分类和审核状态流转，不依赖外网、真实账号或真实 API 密钥。
- 已确认的扩展允许文本附件在明确指令且启用 LLM 时生成待审核草稿；允许通用对话、受控采集与待确认的定时/发布计划。
- 不实现真实定时执行、平台账号登录、自动发布、模拟登录或绕过验证；如需实际平台适配，必须指定目标平台并重新确认范围。

## LLM 与内容安全

- LLM 通过 OpenAI 兼容接口接入，模型名、基础地址和密钥均从环境变量读取，不写入代码库。
- 没有密钥时应支持固定样例或确定性降级流程，保证离线测试可运行。
- LLM 输出必须经过结构化校验；来源、标题、URL、发布时间等事实字段不能由模型虚构。
- 每篇草稿必须保留原始信息链接和来源名称；证据不足的内容标记为待核实，不写成确定事实。
- 文章分类至少支持论文研究、开源项目、行业新闻、产品更新和日常观察；分类结果应保存置信度并允许人工修改。

## 工程与安全约束

- 采集器遵守来源站点的访问规则、限速和失败退避要求，并提供可配置白名单。
- 文件解析、数据库写入、外部请求和模型调用需要清晰的异常处理及可追踪日志。
- 审核批准操作必须具备幂等性，未经批准的草稿不能进入“待发布”。
- 通用对话只能路由到已登记的意图和 Tool；定时/发布 Tool 必须先创建待确认计划，确认记录不等同于实际执行。
- 前端可展示可审计的执行过程摘要，但不得输出模型原始思维链、内部提示词或密钥。
- 测试不得发起真实发布、产生外部付费调用或覆盖用户已有数据。
- PostgreSQL 仅绑定本机地址，凭据通过环境变量注入，持久化数据使用 Docker 命名卷。
- 保留用户已有文件与配置；不得擅自清理、重置或覆盖无关修改。
