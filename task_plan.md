# Task Plan: 资讯运营 Agent 第一版

## Goal

交付可运行的 FastAPI + LangGraph + PostgreSQL MVP，实现 arXiv、GitHub Trending、Hacker News、官方 RSS 到可审核中文草稿的完整闭环。

## Current Phase

Phase 57 complete

## Phases

### Phase 1: 项目配置、领域模型、PostgreSQL 与 Alembic

- [x] 配置依赖与环境变量
- [x] 建立领域对象和 PostgreSQL 表
- [x] 创建 Alembic 初始迁移
- **Status:** complete

### Phase 32: 证据包收敛、DeepAgent 启用与无文字配图质量门

- [x] 解析 GitHub README JSON，仅向生成器提供长度受限的纯文本内容。
- [x] 将 `DEEP_AGENT_ENABLED` 注入 app 容器，并验证容器实际开关。
- [x] 支持“采集/生成文案 + 自动配图”复合指令进入同一后台工作流。
- [x] 使用本地 OCR 检测生成图片的可见文字，最多重试两次后拒绝绑定草稿。
- [x] 更新项目 Skill、离线测试、依赖锁定、容器验证和过程记录。
- **Status:** complete (容器镜像后台构建中，尚未替换运行容器)

### Phase 33: 原生成记录重生成与模型失败可诊断化

- [x] 通过文字阶段审计中的 `draft_ids` 找回草稿原生成记录，兼容历史记录，无需数据库迁移。
- [x] 将“重新获取/重新生成当前文章”路由为原记录内的后台尝试，而非新建生成记录或草稿。
- [x] 重生成仅更新原草稿的可编辑内容并递增版本，保留来源、图片、审核和发布审计。
- [x] 将自动审核/自动改稿的模型不可用原因分类为配置、鉴权、模型、网络或超时，不记录密钥。
- [x] 更新生成记录状态、离线测试、运行日志与容器验证。
- **Status:** complete

### Phase 34: 长文模型独立超时与连通性验证

- [x] 将正文生成、自动改稿、自动审核和配图规划切换到独立模型超时。
- [x] 限制长文模型 SDK 的单任务重试，避免重复等待。
- [x] 在运行中的 Worker 验证模型接口与 JSON 输出协议连通性。
- [x] 完成容器运行时配置与静态回归检查。
- **Status:** complete

### Phase 35: 会话任务上下文桥接

- [x] 将零新增采集结果写入当前会话摘要，不清空已有活动草稿。
- [x] 为会话上下文 Tool 增加当前会话最近消息，保持严格会话隔离。
- [x] 明确 DeepAgent 对最近任务追问必须先读取受控上下文。
- [x] 完成离线测试与 Worker 镜像重启验证。
- **Status:** complete

### Phase 2: 四类采集器及统一数据格式

- [x] 实现 arXiv Atom 采集器
- [x] 实现 GitHub daily/weekly Trending 采集器
- [x] 实现 Hacker News 官方 API 与可配置 RSS 采集器
- **Status:** complete

### Phase 3: 内容处理与 LangGraph

- [x] 实现清洗、精确去重、分类和热度计算
- [x] 实现可选 OpenAI 兼容生成器和确定性降级
- [x] 实现 LangGraph 处理管道
- **Status:** complete

### Phase 4: FastAPI、审核状态与 Docker

- [x] 实现采集、查询、编辑和审核接口
- [x] 实现请求追踪与幂等审核
- [x] 实现 Docker Compose 和 PostgreSQL/pgvector 部署
- **Status:** complete

### Phase 5: 测试、容器验证与文档

- [x] 完成固定样例和离线测试
- [x] 完成迁移、健康检查和审核闭环验证
- [x] 完成 README 和过程记录
- **Status:** complete

### Phase 6: 采集运行记录与 GitHub Trending 快照

- [x] 新增采集运行和趋势快照表及迁移
- [x] 实现各来源失败隔离和运行审计
- [x] 提供来源健康与趋势查询接口
- **Status:** complete

### Phase 7: 离线与真实来源验收

- [x] 增加运行记录、快照变化和失败隔离测试
- [x] 验证真实 arXiv、GitHub、HN 和官方 RSS
- [x] 重建容器、运行迁移并更新文档
- **Status:** complete

### Phase 8: 受控主 Agent 与内部采集 Tool

- [x] 定义四个只读采集 Tool 的统一输入、输出和错误结构
- [x] 增加主 Agent 的显式状态、允许来源与执行决策
- [x] 为“采集全部 / 指定来源 / 查询来源状态”提供受控入口
- [x] 增加 Tool 与主 Agent 的离线测试及使用文档
- **Status:** complete

### Phase 9: 对话附件、MinIO 与审核运营台

- [x] 创建 React + Vite + TypeScript + shadcn/ui 风格前端工程，提供对话与审核页面
- [x] 增加私有 MinIO、文本附件元数据和受控下载接口
- [x] 实现“仅明确提取指令才允许 LLM/Tool 处理附件”的权限门禁
- [x] 完成附件、审核页和前后端闭环的离线与容器验收
- **Status:** complete

### Phase 10: 受控通用对话、意图识别与计划 Tool

- [x] 建立通用对话意图、运行审计和可折叠执行摘要的数据模型
- [x] 实现 ConversationAgent 及采集、定时计划、发布计划的受控 Tool
- [x] 增加待确认定时/发布计划 API；不接入真实调度器或发布平台
- [x] 更新对话页的执行过程摘要、确认计划和测试
- [x] 完成离线与容器验收并更新文档
- **Status:** complete

### Phase 11: 聚合资讯任务、会话历史与受控持久记忆

- [x] 修复 LLM 草稿结构兼容与失败审计
- [x] 实现 GitHub 并行采集、前五项聚合和单条总文案证据链
- [x] 增加 Redis/ARQ 后台任务与前端轮询状态
- [x] 增加聊天会话列表、切换、恢复和显式新建对话
- [x] 修复后台任务事件序号冲突并验证成功/失败状态回写
- [x] 完成证据驱动增强生成第一阶段与受控 Tool/Skill 基础分层
- [x] 修正来源全失败时聊天任务仍显示“采集完成”的状态汇总与可见错误提示
- [x] 配置 Docker 出站代理与本地根证书信任链，并验证 Worker 可访问 GitHub Trending
- [x] 修复 GitHub 聚合工作流未保存草稿生成器导致的生成失败，并补充回归测试
- [ ] 接入受控 Deep Agents 记忆适配与 PostgreSQL 持久存储
- [ ] 完成迁移、离线测试、容器验证和文档
- **Status:** in_progress

### Phase 12: 单项目深度资讯与来源正文富化

- [x] GitHub Trending 候选先经数据库介绍历史筛选，再对单个项目读取 README。
- [x] 保存项目介绍历史，草稿创建成功后才写入记录；已有旧草稿同样不重复选题。
- [x] Hacker News 抽取安全公开外链的完整正文，并限制响应大小和并发。
- [x] LLM 只接收受上限保护的证据摘录，生成约 450–650 字待审核文案。
- [x] 完成 `0008_project_introductions` 迁移、离线测试和容器健康检查。
- **Status:** complete

### Phase 13: 人工发布回填与介绍历史校正

- [x] 将 GitHub 已介绍记录的写入时机从草稿生成调整为人工发布回填成功。
- [x] 新增发布回填审计、`published` 草稿状态与幂等链接记录。
- [x] 将旧预发布介绍记录保留为审计数据但排除在候选去重之外。
- [x] 完成人工审核页回填入口、迁移、离线测试、前端构建和容器验证。
- **Status:** complete

### Phase 14: 公众号 MCP 受控发布与发布情况

- [x] 部署仅 Docker 内网可访问的公众号 MCP 服务，并注入用户本地凭据。
- [x] 增加公众号草稿、提交、状态查询与图片素材上传的最小 MCP 适配器。
- [x] 保存公众号发布任务审计，并提供“发布情况”查询页。
- [x] 图片默认仅私有保存；只有在发布页明确选择后才能作为封面或正文插图上传。
- [x] 完成迁移、离线测试、前后端构建与容器健康验证；不创建真实公众号草稿或发布。
- **Status:** complete

### Phase 15: 发布素材库、纯文本预览与多图发布体验

- [x] 新建独立发布素材库，支持上传、列出与安全删除未引用图片。
- [x] 发布页面支持多文件上传、封面/正文图片选择与发布前预览。
- [x] 审核文案保持纯文本，公众号适配仅在 API 边界生成最小传输 HTML。
- [x] 完成迁移、离线测试、前端构建与容器运行验证；不创建真实公众号草稿或发布。
- **Status:** complete

### Phase 16: 公众号 MCP SSE 兼容性修复

- [x] 在项目自有构建层验证并应用第三方 SSE 重复响应头兼容补丁。
- [x] 重建 MCP 容器，并仅验证 app 可收到 SSE endpoint；不调用公众号 Tool。
- **Status:** complete

### Phase 17: 公众号 MCP 消息端点兼容性修复

- [x] 在项目自有构建补丁中增加 SSE 会话映射及 `POST /messages` 转发。
- [x] 优化应用端对 MCP 消息端点 404 的安全提示，并补充专项测试。
- [x] 重建 MCP 容器，只验证本地 `initialize` 协议往返；不调用公众号 Tool。
- **Status:** complete

### Phase 18: 公众号 MCP 图片请求体限制修复

- [x] 在项目自有构建补丁中将 JSON 解析上限限制为 `4mb`，匹配前端单图 2 MB 边界及 Base64 开销。
- [x] 重建 MCP 容器，并以无会话的大请求体确认解析器不再返回 `entity.too.large`；不调用公众号 Tool。
- **Status:** complete

### Phase 19: 人工审核废弃状态提示去重

- [x] 废弃成功后仅显示草稿状态提示，不再重复显示临时操作提示。
- [x] 完成前端生产构建验证。
- **Status:** complete

### Phase 20: 生成记录、AI 审核与受控插图生成

- [x] 新会话在收到首条真实用户指令后，以其首句生成可读标题；附件上传本身不改写标题。
- [x] 建立项目内 `image-generation` 与 `auto-review` Skill：前者使用供应商可配置的图片生成 Tool，后者只核验已生成草稿与证据；两者均不携带密钥。
- [x] 新增草稿插图与自动审核运行审计表、迁移、仓储和 API；插图可关联私有发布素材并指定插入段落。
- [x] 对话增加“生成插图”受控意图与“自动审核”请求标记；仅在用户显式提出时调用图片服务。
- [x] AI 审核同时执行固定规则和结构化模型核验；通过后可自动创建公众号草稿，但绝不提交发表。
- [x] 按已确认 A 策略：自动审核通过且缺少封面时，按文章内容生成一张通用科技插画作为封面；图片服务、自动草稿开关或公众号服务未配置时安全失败并保留审计。
- [x] 前端将“人工审核”改为“生成记录”，展示标签、插图位置、自动审核与公众号草稿状态；聊天区提供自动审核勾选。
- [x] 增加离线测试、前端生产构建、数据库迁移和容器健康验证；不调用真实模型、图片服务或公众号接口。
- **Status:** complete

### Phase 21: Agent 决策式自动配图

- [x] 对话区新增独立“自动配图”勾选，并持久记录到聊天 Agent 运行。
- [x] 新增受控配图规划 Tool：仅基于生成后的草稿，决定 0–3 张图及段落位置；LLM 不可用时使用可审计的确定性降级策略。
- [x] 后台采集先执行配图，再执行可选自动审核；配图只私有保存，审核通过后的公众号草稿使用已生成图片。
- [x] 补充迁移、专项离线测试、前端构建、容器迁移与健康验证；不调用真实模型、图片服务或公众号接口。
- **Status:** complete

### Phase 22: 受控附件工作区与生成过程状态

- [x] 让会话附件在 MinIO 私有原件之外，保存一份受控 Agent 工作区副本，并在会话删除时同步清理。
- [x] 仅在用户明确引用附件时将其作为文本或视觉上下文传入；支持“设为封面”及“插入第 N 段后”的受控图片绑定。
- [x] 为后台生成写入文字、配图规划、逐图生成、自动审核和草稿投递阶段事件。
- [x] 在生成记录中持久展示运行中任务及文字/图片分项状态，并补充离线、迁移、构建与容器验证。
- **Status:** complete

### Phase 23: 生成队列详情、审核改稿重试与术语解释

- [x] 将运行中的任务并入生成记录左侧队列，选择后在右侧展示阶段详情与封面图。
- [x] 修复文字、配图与自动审核终态映射，区分“审核未通过”与系统执行失败。
- [x] 自动审核未通过时，最多两次基于审核意见的受控改稿和复审，并保留每轮审计。
- [x] 更新文案生成与改稿规则：首次出现的原文术语必须保留，并在括号内给出翻译或面向普通读者的解释。
- [x] 补充离线测试、前端构建、迁移与容器/API 验证；未触发真实模型、图片或公众号接口。
- **Status:** complete

### Phase 24: 独立图片后台任务与发布状态队列

- [x] 将图片生成拆为单图片、可审计、可按任务 ID 查询的 ARQ 子任务。
- [x] 为采集、图片与收束 Worker 配置独立超时；图片服务请求 15 分钟，单图任务 20 分钟。
- [x] 在取消超时时回写父聊天任务失败状态，避免永久显示“文字生成中”。
- [x] 所有图片子任务终态后才执行自动审核；自动流程仍最多创建公众号草稿，不发表。
- [x] 生成记录显示每个图片任务 ID、类型、位置、状态与可见失败原因。
- [x] 发布情况增加按日期分组的文章列表、选中详情和投递状态展示。
- [x] 完成离线测试、前端构建、Compose 校验和数据库迁移；发布列表/逐任务轮询的最后前端改动已通过 TypeScript 静态检查，待重建前端容器；最终容器 HTTP 复查受当前执行权限限制未重复执行。
- **Status:** complete

### Phase 25: 生成失败可见性与文章级发布素材

- [x] 将生成运行的失败终态与原因统一纳入生成记录队列，修复 0 条草稿被误标为完成和终态回执选取错误。
- [x] 对模型额度、超时、限流及服务端故障使用来源受限的确定性草稿降级，并明确标记降级模式；结构与事实校验失败不降级。
- [x] 为公众号 MCP 增加不含凭据的错误类别与返回码审计，便于区分 MCP、网络、凭据与微信授权问题。
- [x] 新增文章—发布素材绑定表；当前文章只显示其显式上传或 Agent 生成的素材，旧全局素材不回填、不删除。
- [x] 调整发布页三栏比例，缩窄文章列表并加宽预览；完成离线测试、前后端构建和迁移验证。
- **Status:** complete

### Phase 26: 全局失败通知中心

- [x] 新增可读、可隐藏的失败通知记录，并一次性补齐既有失败审计。
- [x] 将文案生成、图片任务、公众号素材/草稿/提交/查询失败写入统一通知。
- [x] 在全局右上角实现未读红点、详情展开、跳转、单条已读/删除与全部已读。
- [x] 失败通知删除不会删除任务、草稿、审核、图片或公众号投递审计。
- [x] 完成离线测试、前端构建、容器重建、迁移与本地接口验证。
- **Status:** complete

### Phase 27: 图片回退适配与记录列表精简

- [x] 在图片服务返回空 Base64 时回退下载受限 HTTPS 图片 URL，避免 0 字节图片进入保存流程。
- [x] 增加空 Base64 与 URL 回退的离线回归测试。
- [x] 生成队列与发布文章列表增加视口限高及滚动条。
- [x] 失败生成任务移入按日期生成记录列表，运行中任务仍单列显示。
- [x] 移除发布页重复的已审核文案下拉框，统一由文章列表选择。
- [x] 完成离线测试、前端构建和容器 HTTP 验证。
- **Status:** complete

### Phase 28: 废弃草稿可重新生成

- [x] 移除来源与草稿的一对一数据库限制，保留废弃历史后允许同来源生成新草稿。
- [x] 将采集去重改为仅由未废弃/未删除或已发布草稿阻止。
- [x] 补充废弃后重生成与有效草稿仍去重的回归测试，并验证迁移与容器。
- **Status:** complete

### Phase 29: Worker 图片存储与配图失败审核边界

- [x] 为 Worker 注入 Docker 内网 MinIO 连接配置，避免容器内误连 `localhost:9000`。
- [x] 图片任务失败或超时时保留文字草稿，跳过自动审核通过与公众号草稿投递。
- [x] 显示“配图未完成”“自动审核待人工处理”的可审计状态，并完成离线与容器连通性验证。
- **Status:** complete

### Phase 30: 自动配图与插图定位 Skill

- [x] 新建自动配图规则包，明确位置规划、后台图片任务、私有保存和草稿插图绑定的顺序。
- [x] 将单图生成 Skill 的职责收敛到自动配图工作流的子步骤。
- [x] 补充“先规划、再逐位置生成与绑定”的离线回归测试。
- **Status:** complete

### Phase 31: DeepAgent 持久记忆与受控能力重构

- [x] 以 `create_deep_agent` 接管会话编排；关闭默认 Shell、任意文件和默认子代理能力。
- [x] 使用 PostgreSQL checkpoint 与结构化会话记忆保存当前草稿、附件引用和摘要。
- [x] 将采集、草稿定位、配图、计划、附件与登记脚本能力重组为可审计 Tool，补齐对应项目 Skill。
- [x] 将 Exa/公众号保留在 MCP 适配层；不将发表能力交给对话 Agent。
- [x] 图片失败/超时中断自动审核和公众号草稿投递；图片提示词禁止嵌入文字。
- [x] 优化发布页三步按钮布局并完成离线、构建、迁移与容器验证。
- **Status:** complete

### Phase 51: 发布页审核兼容、来源写作 Skill 与生成体验

- [x] 将自动审核意见统一为可显示的文本，并兼容历史 `{type, description}` 结构，恢复发布页面。
- [x] 新建 arXiv、GitHub、Hacker News、官方 RSS 四个来源专用写作 Skill，并由生成器按来源加载。
- [x] GitHub 文案标题强制包含仓库项目名；正文最低 1200 字符，并允许受控 Exa 搜索补充背景、技术优势与部署语境。
- [x] 优化文字、配图、自动审核三阶段的进行中、完成、失败、等待视觉状态。
- [x] 清理过时的附件默认保存提示，不改变附件的实际授权边界。
- [x] 补充后端、前端与 Skill 加载回归验证，重建本地容器；不发起真实模型、搜索、图片或公众号调用。
- **Status:** complete

### Phase 52: 来源写作 Skill 参考资料化

- [x] 为四个来源写作 Skill 增加按需读取的证据与联网补充参考资料。
- [x] 将入口 Skill 精简为明确路由，不新增未接入运行链的占位脚本。
- [x] 核对生成器读取路径、Skill 校验与容器镜像包含关系。
- [x] 评估联网搜索改为会话子 Agent Tool 的收益、风险与现有受控 Tool 的差异；不实施该架构变更。
- **Status:** complete

### Phase 53: 异步人工触发审核与 GitHub 正文收敛

- [x] 将手动“运行自动审核”改为仅创建审核运行并入 ARQ 队列；HTTP 请求不等待模型、改稿或草稿箱投递。
- [x] 让 Worker 执行审核工作流，并将阻塞式模型调用移出 Worker 事件循环；保留审核、改稿与投递审计。
- [x] 拆分封面与正文插图的素材限制提示，明确正文插图 1 MB 是本地预检而非封面限制。
- [x] 对 GitHub 来源取消正文“原文标题”尾注，并同步生成、改稿、重生成和审核规则。
- [x] 补充回归测试、前端状态反馈、静态检查与容器运行态验证；不调用真实模型或微信公众号接口。
- **Status:** complete

### Phase 54: 受限文案与审核子 Agent

- [x] 将正文生成的内容判断封装为无副作用的受限 DeepAgent 子能力，并保留来源证据、Pydantic 校验与受控联网搜索边界。
- [x] 将自动审核的模型判断封装为无副作用的受限 DeepAgent 子能力；硬规则、通过阈值、改稿和状态迁移仍在编排服务中执行。
- [x] 不授予子 Agent 数据库写入、删除、图片服务或微信公众号投递 Tool；保持现有任务模型路由与超时配置。
- [x] 补充替身测试、静态编译、容器装配及运行态验证；不调用真实模型、搜索、图片或公众号接口。
- **Status:** complete

## Decisions Made

| Decision | Rationale |
|---|---|
| GitHub 采集全站 daily/weekly Trending，各取前 25 | 用户确认不采用仓库白名单 |
| 使用 `pgvector/pgvector:pg16` | 用户确认 Docker PostgreSQL，并保留语义去重扩展能力 |
| 无密钥时使用确定性生成器 | 离线测试不访问付费模型 |
| 第一版不实现图片、文件、MCP 和发布适配 | 保持已确认的 MVP 边界 |
| 首个官方 RSS 使用 GitHub Changelog | 用户确认 |
| 首先实现项目内部 Tool，不直接建设 MCP Server | 保持部署与测试简单，未来可复用同一 Tool 层提供 MCP 适配器 |
| 文本附件默认仅保存 | 附件内容视为不可信输入；仅明确提取指令可进入 LLM/Tool 工作流 |
| 定时/发布为受控 Tool，规则为可复用 Skill | Tool 承担有输入输出和审计的状态操作；Skill 仅约束何时能调用及必须确认 |
| GitHub 资讯按前 5 项聚合为一条草稿 | 避免对十余条项目串行生成，保留全部证据链接并降低模型调用数 |
| GitHub 资讯改为单项目深度草稿 | 用户认为五个项目一篇信息过载；候选先去重，再为单个项目读取 README 和生成约 500 字文案 |
| 仅已发布项目参与 GitHub 去重 | 审核通过不等于已发布；仅运营人员回填真实发布链接后才标记“已介绍” |
| 后台任务采用 Redis + ARQ | API 立即响应，任务状态可轮询，后续可复用到定时任务 |
| Deep Agents 采用受控适配 | 仅用于会话记忆与受限 Tool 编排，不暴露 Shell、任意文件或真实发布能力 |
| 自动审核默认只创建公众号草稿 | 用户确认自动流程，但未授权自动发表；发表仍由运营人员在公众号后台完成 |
| 自动封面采用供应商可配置图片 Tool | 参考 Agnes 接口契约但不复制外部密钥或脚本；配置缺失时安全拒绝并保留原因 |
| 自动配图数量与位置由受控规划 Tool 决定 | 每篇最多 3 张，只能读取已生成草稿；LLM 不可用时使用固定可审计降级规则 |
| 聊天附件采用 MinIO 原件与受控工作区副本双存储 | 保留私有原件和可审计元数据；仅用户明确引用时，工作区副本可被受限 Tool 读取 |

## Errors Encountered

| Error | Attempt | Resolution |
|---|---|---|
| 受限环境无法读取 Docker 配置或连接引擎 | 1 | 使用获准的只读 Docker 检查，确认 Client/Server 29.7.2 |
| uv trampoline 在受限环境中无法启动 | 1 | 获准后运行，确认是环境权限边界 |
| `.venv` 未安装 pip | 1 | 识别为 uv 管理环境，改用 `uv sync --extra dev` |
| 首次 Docker 构建在基础镜像下载阶段被执行窗口中断 | 1 | 利用缓存拆分 build 和 up 后成功 |
| 清理缓存时递归枚举进入 `.venv`，`.pytest_cache` 访问被拒绝 | 1 | 停止继续清理；仅涉及可再生字节码，源码和数据库卷未受影响 |
| 完成检查脚本未识别简写阶段格式 | 1 | 改为标准 `### Phase` 和 `**Status:** complete` 格式 |
| 受限环境拒绝 uv trampoline 启动项目 Python | 1 | 使用已授权的项目虚拟环境运行，10 项离线测试通过 |
| 真实验收意外读取现有 `.env` 并调用外部 LLM | 1 | 立即重启应用中断请求；新增默认关闭的 `LLM_ENABLED` 显式开关，不修改用户 `.env` |
| 主机和容器均将 `github.com` 解析为 `127.0.0.1` | 1 | 保留失败审计并验证故障隔离；不修改系统 DNS/hosts/代理，解析和快照逻辑由固定样例及事务回滚验收覆盖 |
| 新增 ConversationAgent 测试无法收集 | 1 | 发现测试替身 `list[dict[str, str]]` 类型标注括号拼写错误，已定点修正后重新测试 |
| 受限执行环境无法查询 uv Python 解释器 | 1 | 改为请求使用已授权的项目 uv 环境执行离线编译与测试；不修改项目依赖或用户配置。 |
| Node 20 Bookworm 基础镜像下载在构建窗口内未完成 | 2 | 改用项目已有缓存的 Node 24 Alpine 镜像，并在镜像内安装编译 SQLite 所需工具；Node 24 满足第三方 MCP 的 Node 18+ 要求。 |
| 第三方 MCP 的 npm 依赖安装长时间无构建输出 | 1 | 识别为 Docker 构建阶段没有继承项目既有 `OUTBOUND_*` 出站代理；为构建阶段添加同一组代理参数后，复用已缓存基础层重试。 |
| 发布素材首轮离线测试 2 项失败 | 1 | 图片校验仅判断 MIME 类型集合，未验证扩展名和 MIME 的对应关系；已改为精确匹配。纯文本适配会移除 HTML 标签，测试已改为断言标签不会进入渲染结果。 |
| 发布素材文档更新补丁未匹配 | 1 | README 实际段落为单行，与首次补丁的换行上下文不同；重新读取原文后以精确上下文更新，未影响业务代码。 |
| 新前端首次 HTTP 检查提前结束 | 1 | 容器刚启动时 Vite 尚未监听完成；不重建或修改代码，等待 3 秒后复查返回 HTTP 200。 |
| 前端格式化工具不可用 | 1 | `npx prettier` 尝试下载依赖时因 npm 缓存目录权限失败，未改动前端文件；改用定点补丁，不安装依赖。 |
| 前端离线格式化仍无法写入 | 2 | 本机已有 Prettier 但 Windows 拒绝其直接写入正在使用的源码文件；未改动文件，改为使用 `apply_patch` 增加独立状态组件，避免重写历史单行 JSX。 |
| Worker 专项断言与真实异常阶段不符 | 1 | 采集 Agent 在文字生成开始前抛异常时，Worker 正确记录“执行失败”；仅修正测试期望，不修改失败处理逻辑。 |
| 前端构建首次在错误目录执行 | 1 | 项目根目录没有 `package.json`，命令未开始构建；改在 `frontend/` 目录执行。 |
| 工作区权限探针命令转义错误 | 1 | PowerShell 提前展开了 shell 临时变量，未实际写入探针文件；改用固定路径重试，不根据该结果判断权限。 |
| 新工作区卷不允许 appuser 写入 | 1 | Docker 命名卷初始归 root 所有。新增一次性 root 初始化容器，仅对该专用卷授予 UID/GID 10001 所有权；app/worker 继续非 root 运行。 |
| 工作区初始化容器命令被 Compose 拆分 | 1 | `sh -c` 未接收到完整命令，日志显示 `mkdir: missing operand`；将完整初始化脚本放入 entrypoint 数组，不重复原命令形式。 |
| Phase 23 首次 uv 编译/测试无法查询解释器 | 1 | 受限环境返回“拒绝访问（os error 5）”；改用已授权的同一项目 uv 环境重试，不修改依赖或配置。 |
| MCP 代理排除项首轮修复仍返回 502 | 1 | `.env` 的 `OUTBOUND_NO_PROXY` 覆盖了 Compose 默认值；不修改用户 `.env`，改为在 Compose 结果末尾无条件追加 `wechat-mcp`。 |
| 受限环境无法创建 uv 缓存目录 | 1 | 使用已授权的项目 uv 环境运行相同专项测试，4 项通过；未修改依赖或缓存配置。 |
| MCP endpoint 验证脚本未正确解析网络分片 | 2 | 确认服务已返回 HTTP 200 与 endpoint 事件后，改用无正则的字节分割解析；`initialize` 返回 HTTP 202。 |
| Phase 18 第三方 MCP 镜像构建超时 | 1 | `npm ci` 访问 `registry.npmjs.org:443` 发生 `EIDLETIMEOUT`，新镜像未生成、旧容器保持运行；需要经确认后增加 npm 下载重试与构建缓存。 |
| Phase 18 增加缓存/重试后仍无法完成构建 | 1 | `npm ci` 访问 `registry.npmjs.org` 时出现 `ECONNRESET`；当前镜像与运行容器未替换，需要确认新的网络或镜像源方案。 |

### Phase 36: 聊天即时回显与后台会话回复

- [x] 普通对话改为本地显式意图分流，用户消息先提交并立即返回。
- [x] 将 DeepAgent 会话回复转入 ARQ 后台任务，保留当前会话记忆与事件审计。
- [x] 为聊天 API、入队、Worker、DeepAgent 超时和前端请求补齐结构化日志。
- [x] 前端增加本地发送态与失败态，后台回复继续通过现有轮询刷新。
- **Status:** complete

### Phase 37: 四部分正文生成与宽松字数审核

- [x] 生成与自动改稿改为返回 4 个不可见逻辑部分，每部分允许多个自然段。
- [x] 服务端校验各部分最低字数并保存可审计的字数报告。
- [x] 审核总长度放宽至 800–3200 字符、自然段放宽至 4–8 段。
- [x] 正文、自动改稿、审核统一使用 120 秒长文模型时限并补充阶段日志。
- **Status:** complete

### Phase 38: 会话级 DeepAgent 统一决策

- [x] 让每个会话以 `session_id` 恢复同一 DeepAgent checkpoint，并由该 Agent 返回受限结构化决策。
- [x] 移除路由层的独立意图模型调用；普通对话不再进入第二次 DeepAgent 回复链路。
- [x] 将会话上下文作为受控快照提供给 Agent，限制默认文件工具和工具回合，避免无关多次模型调用。
- [x] 为模型限流、超时和结构化输出异常保留安全分类；不执行业务 Tool。
- [x] 完成静态检查、容器重建与运行态检查；专项 pytest 受本机解释器权限/生产镜像不含 pytest 限制，未替代为外部依赖安装。
- **Status:** complete

### Phase 39: 分级模型配置与调用可观测性

- [x] 为会话、正文、审核和配图规划提供独立模型环境变量，并在未配置时回退至 `LLM_MODEL`。
- [x] 将各调用点切换为按任务选模，并记录模型类别与模型名，不记录正文或密钥。
- [x] 完成静态与容器运行态验证；不发起真实模型调用。
- **Status:** complete

### Phase 40: 分级模型服务地址

- [x] 为四类任务增加独立 OpenAI-compatible Base URL，并在留空时回退统一地址。
- [x] 让会话、正文/改稿、审核和配图规划分别使用其任务地址。
- [x] 完成静态、容器路由与服务存活验证；不发起真实模型调用。
- **Status:** complete

### Phase 41: 分级模型凭据与配置整理

- [x] 为四类任务增加独立 API Key，并在留空时回退统一密钥。
- [x] 将模型、服务地址和密钥按任务重新编排至 `.env` 与示例配置。
- [x] 完成静态、容器路由与服务存活验证；不读取或调用真实密钥。
- **Status:** complete

### Phase 42: Pydantic 会话决策兼容

- [x] 收紧 DeepAgent 的最终 JSON 输出提示词，并继续保留原生 Pydantic `response_format`。
- [x] 原生结构字段缺失时，仅解析最终助手消息中的完整 JSON，再以 `ConversationDecision` 严格校验。
- [x] 拒绝用户消息、非 JSON 与 Schema 不合格结果；完成离线与容器验证。
- **Status:** complete

### Phase 43: 工具式 Pydantic 会话决策

- [x] 识别 OpenAI-compatible 网关未遵从原生 JSON Schema 而在框架内部失败的路径。
- [x] 显式使用 `ToolStrategy(ConversationDecision)`，保留 Pydantic 校验和最终 JSON 兼容解析。
- [x] 完成离线断言、app/worker 重建与不调用真实模型的容器验证。
- **Status:** complete

### Phase 44: GitHub README 草稿期私有快照

- [x] 为每篇草稿建立唯一的私有 README 快照元数据与迁移。
- [x] 仅在 GitHub README 成功获取时写入 MinIO；正文、哈希和对象键不进入前端响应。
- [x] 原记录重生成优先恢复快照，只有无快照时才重新请求 GitHub。
- [x] 人工或自动审核通过后删除对象正文并保留删除时间、哈希、长度和来源审计字段。
- [x] 完成容器迁移、离线回归和运行态验证。
- **Status:** complete

### Phase 45: GitHub 候选前置去重与发布信息架构

- [x] 核对自动审核、公众号草稿创建的实际时序，以及两个页面的职责边界。
- [x] 核对 GitHub 候选的已发布排除、排序、有效草稿去重顺序。
- [x] 只读核对可删除的项目介绍排除名单。
- [x] 将公众号草稿准备移至生成记录；在发布情况集中运行和查看自动审核。
- [x] 将已发布项目和未废弃草稿的 GitHub 仓库排除移至热度排序前。
- [x] 核对历史排除名单为空，未执行数据库删除。
- **Status:** complete

### Phase 46: 遗留生成任务收束与聊天交互

- [x] 核对遗留“正在生成”记录的查询和收束路径。
- [x] 为超过安全时限的运行记录写入失败终态和通知，不删除草稿或审计。
- [x] 将自动配图、自动审核默认开启，并优化其可辨识的开关样式。
- [x] 会话切换、发送和新消息到达时滚动到最新消息。
- [x] 完成静态与回归验证。
- **Status:** complete

### Phase 47: 遗留任务处理与生成队列删除

- [x] 只读核对遗留运行记录、关联草稿、图片任务与卡住阶段。
- [x] 部署超时收束逻辑并将指定遗留记录安全结束为失败。
- [x] 增加仅删除生成任务审计的后端接口和生成记录操作入口。
- [x] 验证收束不影响关联草稿与插图；删除的审计边界由回归用例覆盖。
- **Status:** complete

### Phase 48: 内容模型超时直报与原记录重试

- [x] 将模型服务故障由确定性兜底正文改为安全失败原因。
- [x] 将内容模型和后台文字任务默认等待值分别调整为 600 秒和 900 秒。
- [x] 新增失败生成记录的原记录重试 API 与前端按钮。
- [x] 完成 Python 语法、既有回归、前端类型与容器路由验证。
- [ ] 等待用户将真实 `.env` 中的显式超时值更新为建议值后，复核运行时配置。
- **Status:** awaiting configuration

### Phase 49: 自动素材决策、阈值审核与草稿箱终态

- [x] 只读核对当前容器、后台任务、最新草稿、审核及公众号草稿创建状态。
- [x] 由模型决定封面与插图，移除发布页要求人工逐张勾选的路径。
- [x] 让自动审核在一次评审中输出完整问题清单、结构化评分和通过阈值，并限制自动改稿轮次。
- [x] 将“公众号草稿已创建”设为个人账号流程终点，并在成功后写入对应去重记录。
- [x] 确认四类来源均在草稿创建成功后去重，完成回归与容器验证；现有表结构足够，无需新增迁移。
- **Status:** complete

### Phase 50: 公众号白名单诊断、草稿箱提示与图文预览

- [x] 从 MCP 容器日志确认上传失败的认证、权限和网络边界。
- [x] 将微信接口 `40164` 分类为可操作的 IP 白名单提示，不暴露出口 IP 或凭据。
- [x] 将草稿箱投递失败提示从自动审核区移至当前草稿箱投递区。
- [x] 在草稿预览中按封面及正文插图实际位置渲染完整图文效果。
- [x] 为微信公众号 MCP 增加独立的运行时直连配置，并验证容器代理变量为空。
- [x] 完成 Python 语法、前端类型/生产构建、容器替身和 HTTP 健康验证。
- **Status:** complete

### Phase 51: 图片重生成、草稿箱重投与本次尝试收束

- [x] 将 Agnes Image 2.5 Flash 的固定接口契约整理到运行时 image-generation Skill reference。
- [x] 统一生成、改稿和审核的常见术语说明边界。
- [x] 修复预览固定高度截断，并为草稿箱失败增加重新投递入口。
- [x] 让重生成的自动配图替换旧系统图片、复用模式保持已有图片，并分配差异化视觉方向。
- [x] 让生成记录收束仅判断当前尝试的图片任务与事件，避免历史 finalization 阻断。
- [x] 完成容器构建、前后端验证及当前遗留运行的无外部调用状态修复。
- **Status:** complete

### Phase 55: 本地微信官方接口 Skill/Tool 迁移

- [x] 核对现有公众号 MCP 调用点、本地官方接口 Skill 脚本及已验证权限边界。
- [x] 让应用通过本地 Skill 脚本对应的 Tool 直接调用微信官方 REST API，并安全映射错误。
- [x] 替换自动投递和草稿箱读取路径，移除应用与 Worker 对 MCP/SSE 的依赖；已发表内容保持不可用边界。
- [x] 更新可迁移配置、Skill 文档与专项测试；不读取私有 `.env`，不创建远端草稿或发布文章。
- [x] 重建 app/worker，验证本地 Tool 装配、HTTP 健康和 Compose 不再启动 MCP。
- **Status:** complete

## Errors Encountered

| Error | Attempt | Resolution |
|---|---:|---|
| `uv run` 与项目 `.venv` 均因 `os error 5` 无法启动解释器 | 1 | 改为静态读取已安装 Deep Agents 源码；不改依赖或环境变量。 |
| Anaconda 运行测试缺少项目依赖且既有 `.pytest_cache` 不可写 | 1 | 不安装依赖、不清理缓存，改用 Docker 应用镜像执行隔离测试。 |
| Skill Creator 校验器在 Windows 默认 GBK 下读取 UTF-8 中文文件失败 | 1 | 使用 `PYTHONUTF8=1` 重跑，四个来源 Skill 均通过校验；不修改校验器或 Skill 编码。 |
| 根目录 `npx tsc` 触发临时下载且 npm 缓存目录无写权限 | 1 | 不安装或清理 npm；改用前端本地 `node_modules` 的 TypeScript 二进制执行无缓存检查。 |
| 宿主 Python 收集专项测试缺少 `minio` 与 `psycopg` | 1 | 不改宿主依赖；改用现有 Docker 应用镜像或仅做容器内替身验证。 |
| 宿主专项 pytest 缺少 `fastapi`，应用容器不含 pytest | 1 | 不安装依赖；用 Python 静态编译、容器内本地 Skill 装载及替身投递断言验证。 |
| 直接运行 pytest 时 `runtime_logs/news-agent.log` 被运行中的 app/worker 容器占用 | 1 | 不停止容器；把 `LOG_FILE` 指向临时路径后再运行，并在 README 记录该注意事项。 |
| pytest `tmp_path` 基目录在沙箱临时区被拒绝创建，2 项用例 setup 报错 | 1 | 用 `--deselect` 跳过并改在项目内临时目录执行等价手工脚本，两项逻辑均验证通过。 |
| `--basetemp` 指向项目内目录后 teardown 清理被沙箱拒绝 | 1 | 不再使用 `--basetemp`，改回 `--deselect` 方案；已停止重复该路径。 |
| Python 就地重写 `frontend/src/App.tsx` 被沙箱拒绝（文件含超长单行 JSX） | 1 | 按沙箱提示申请一次更宽权限完成锚点删除；不再重复尝试同类写入。 |
| `vite build` 因 esbuild 以管道 stdio 启动子进程报 `spawn EPERM` | 1 | 属沙箱管道边界；申请一次更宽权限后构建成功（1580 模块）。 |
| `process.md` 首次写入返回 `ReplaceFileW EIO (Win32 1175)` | 1 | 重新读取文件确认内容未变后重试成功；未触发第二次同类失败。 |

### Phase 56: 结构化输出双模式（思考模式模型兼容）

- [x] 新增 `LLM_STRUCTURED_OUTPUT_MODE` 与 `CONVERSATION_/CONTENT_/REVIEW_STRUCTURED_OUTPUT_MODE`，留空逐级回退，旧配置行为不变。
- [x] `json` 模式不发送 `response_format`（即不发送 `tool_choice`），改为按模式注入互斥输出指令，并只用本地 Pydantic 校验最终 JSON。
- [x] 校验失败明确报错：不降级生成替代内容，也不执行业务操作。
- [x] 补充模式解析、文案/审核 JSON 模式与会话 JSON 模式回归用例。
- **Status:** complete

### Phase 57: 测试、配置与前端死代码一致性整理

- [x] 将 11 项滞后测试对齐当前行为（GitHub 尾注、1200 字下限、无兜底生成、仓储投递查询、Worker 构造与文案）。
- [x] 删除 `frontend/src/App.tsx` 中 394 行未引用的遗留组件及其失效 API 方法与类型。
- [x] 同步 `.env` 图片模型并注释停用的 `WECHAT_MCP_*` 变量；补充 `.env.example` 与 Compose 透传。
- [x] 更新 README：结构化输出模式、离线测试注意事项、已实现能力与“明确不包含”清单。
- **Status:** complete

